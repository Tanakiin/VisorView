<UserControl x:Class="VisorView.TabItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="36" Height="30">

    <Grid Width="36" Height="30">
        <Border x:Name="ColorChip"
                Margin="2,2,10,2"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="#33FFFFFF"/>

        <Button Content="×"
                Width="14" Height="14"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Black"
                Opacity="0.7"
                Margin="2,3,0,0"
                FontSize="8"
                FontWeight="SemiBold"
                Style="{StaticResource VisorMiniIconButton}"
                Click="Close_Click"/>
    </Grid>
</UserControl>
